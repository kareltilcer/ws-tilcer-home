# The container entrypoint must stay LF even when checked out on Windows, or the
# Alpine /bin/sh shebang line breaks (`\r` in the interpreter path).
*.sh text eol=lf
docker-entrypoint.sh text eol=lf
